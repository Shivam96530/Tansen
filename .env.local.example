# Frontend environment (copy to .env.local)
VITE_API_BASE_URL=http://localhost:5001
VITE_STREAM_BASE_URL=http://localhost:5002

# Optional — enables live Hugging Face sentiment + text generation.
# Without it, a local keyword engine provides the same mood flow.
VITE_HUGGING_FACE_API_KEY=
